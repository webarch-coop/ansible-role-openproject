---
- name: Converge
  become: false
  connection: local
  gather_facts: true
  hosts:
    - localhost
  tasks:

    - name: Include Role
      ansible.builtin.include_role:
        name: openproject
...
